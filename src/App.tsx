// import { useRef } from "react";

function App() {
  // const checkboxRef = useRef<HTMLInputElement>(null);

  return (
    <div className="flex justify-center items-stretch h-screen gap-3 flex-col"></div>
  );
}

export default App;
